{
  "name": "yzmm-status-worker",
  "version": "1.0.0",
  "description": "YZMM Status API - Cloudflare Worker",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
